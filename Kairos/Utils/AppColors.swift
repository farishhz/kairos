import AppKit
import SwiftUI

extension Color {
    static let kairosBackground = Color(red: 36 / 255, green: 36 / 255, blue: 36 / 255)
    static let kairosSidebarBackground = Color(red: 31 / 255, green: 31 / 255, blue: 31 / 255)
}

extension NSColor {
    static let kairosBackground = NSColor(
        calibratedRed: 36 / 255,
        green: 36 / 255,
        blue: 36 / 255,
        alpha: 1)
}
